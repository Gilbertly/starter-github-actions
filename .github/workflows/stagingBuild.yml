name: stagingBuild

on:
  push:
    branches: [main, master]

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Echo message
        run: echo "Deploy to staging, await prod approval"

# install deps

# build staging

# deploy staging

# prod approval hold

# deploy prod
